<div>
  {{#each posts}}
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <p class="card-text">{{body}}</p>
  <p>
    {{User.username}}
    </p>
    <a href="#" class="btn btn-primary">View Blog</a>
  </div>
</div>
{{/each}}
</div>
